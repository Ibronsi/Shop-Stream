## Packages
uuid | For generating unique session IDs on the client side
framer-motion | For smooth page transitions and micro-interactions

## Notes
- Session ID is generated on client start and stored in localStorage
- Cart operations depend on sessionId
- Images will use Unsplash placeholders if dynamic URLs are missing
